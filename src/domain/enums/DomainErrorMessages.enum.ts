/**
 * @enum {DomainErrorMessagesEnum} Enum representing error messages used in the domain.
 *
 * @arg {string} UnexpectedError - An unexpected error message.
 *
 */

export enum DomainErrorMessagesEnum {
  UnexpectedError = 'Something went wrong. Try again later.',
}
