/**
 * @enum {DomainErrorNamesEnum} Enum representing error names used in the domain.
 *
 * @arg {string} UnexpectedError - The name for unexpected errors.
 */

export enum DomainErrorNamesEnum {
  UnexpectedError = 'UnexpectedError',
}
